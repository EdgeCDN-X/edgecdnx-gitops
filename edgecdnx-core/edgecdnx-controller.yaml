---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: edgecdnx-controller
  namespace: argocd
spec:
  goTemplate: true
  syncPolicy:
    preserveResourcesOnDeletion: false
  generators:
    - clusters:
        values:
          chart: edgecdnx-controller
          chartVersion: 0.21.0
          chartRepository: https://edgecdn-x.github.io/helm-charts/
          namespace: edgecdnx-controller-system
          role: cache-controller
          webhooks: "false"
        selector:
          matchExpressions:
            - key: edgecdnx.com/caching
              operator: In
              values:
                - "true"
                - "yes"
    - clusters:
        values:
          chart: edgecdnx-controller
          chartVersion: 0.21.0
          chartRepository: https://edgecdn-x.github.io/helm-charts/
          namespace: edgecdnx-controller-system
          role: router
          webhooks: "false"
        selector:
          matchExpressions:
            - key: edgecdnx.com/routing
              operator: In
              values:
                - "true"
                - "yes"
    - clusters:
        values:
          chart: edgecdnx-controller
          chartVersion: 0.21.0
          chartRepository: https://edgecdn-x.github.io/helm-charts/
          namespace: edgecdnx-controller-system
          role: controller
          targetNamespace: "edgecdnx"
          webhooks: "true"
        selector:
          matchExpressions:
            - key: edgecdnx.com/control
              operator: In
              values:
                - "true"
                - "yes"
  template:
    metadata:
      name: edgecdnx-{{ .values.role }}{{ if ne .name "in-cluster" }}-{{ .name }}{{ end }}
    spec:
      project: default
      sources:
        - chart: "{{ .values.chart }}"
          repoURL: "{{ .values.chartRepository }}"
          targetRevision: "{{ .values.chartVersion }}"
          helm:
            releaseName: edgecdnx-controller
            ignoreMissingValueFiles: true
            values: |
              role: "{{ .values.role }}"
              targetNamespace: '{{ .values.targetNamespace | default "edgecdnx" }}'
              webhook:
                enabled: "{{ .values.webhooks }}"
              {{- if eq .values.role "router" }}
              probes:
                enabled: "true"
                probeUrl: edgecdnx-blackbox-exporter-prometheus-blackbox-exporter.monitoring.svc.cluster.local:9115
                releaseLabel: edgecdnx-monitoring
              {{- end }}
      destination:
        namespace: "{{ .values.namespace }}"
        server: "{{ .server }}"
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true # Big controller.
      ignoreDifferences: []
